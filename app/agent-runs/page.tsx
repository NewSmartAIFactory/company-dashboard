import{Activity,ArrowLeft,Bot}from"lucide-react";import Link from"next/link";import{getAgentRuns}from"../lib/api";import{StatusBadge}from"../components/StatusBadge";
export default async function AgentRunsPage(){const runs=await getAgentRuns();return <main className="detailShell"><Link className="backLink" href="/"><ArrowLeft size={16}/> Back to control center</Link><header className="detailHeader"><div><p className="eyebrow">Agent Execution</p><h1>Agent Run Logs</h1></div><Bot size={28}/></header><section className="panel"><div className="panelHeader"><h2>Recent Runs</h2><Activity size={20}/></div><div className="eventList">{runs.length===0?<p className="emptyState">No agent runs recorded yet.</p>:runs.map(r=><article className="eventItem" key={r.id}><div><strong>{r.agentId}</strong><p>{r.taskId??"Unassigned task"} · {r.inputText}</p><span>{r.outputText??r.error??"Still running"}</span>{r.decisionRequested&&<p>Decision requested: {r.decisionRequested}</p>}<small>{r.filesTouched.join(" · ")}</small></div><div className="auditMeta"><StatusBadge status={r.status}/><span>{r.durationMs?`${r.durationMs} ms`:"—"}</span><time>{new Date(r.startedAtUtc).toLocaleString("en-GB")}</time></div></article>)}</div></section></main>}
