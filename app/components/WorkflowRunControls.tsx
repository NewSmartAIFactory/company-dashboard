"use client";import{useRouter}from"next/navigation";import{useState}from"react";import{companyApiBaseUrl,WorkflowRun}from"../lib/api";
export function WorkflowRunControls({run}:{run:WorkflowRun}){const router=useRouter();const[busy,setBusy]=useState(false);async function advance(){setBusy(true);await fetch(`${companyApiBaseUrl}/api/workflow-runs/${run.id}/advance`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({actor:"PM",note:"Advanced from workflow dashboard"})});setBusy(false);router.refresh();}return <button className="primaryButton" disabled={busy||run.status!=="Running"} onClick={advance}>{busy?"Advancing…":run.status==="Running"?"Advance step":"Completed"}</button>}
