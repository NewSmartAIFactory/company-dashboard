import{ArrowLeft,BookOpen}from"lucide-react";import Link from"next/link";import{getMemories}from"../lib/api";
export default async function MemoryPage(){const memories=await getMemories();return <main className="detailShell"><Link className="backLink" href="/"><ArrowLeft size={16}/> Back to control center</Link><header className="detailHeader"><div><p className="eyebrow">Company Memory</p><h1>Memory Browser</h1><p className="detailSubtitle">Searchable company, project, agent, and decision knowledge.</p></div><BookOpen size={28}/></header><section className="panel"><div className="panelHeader"><h2>Recent Memories</h2><span>{memories.length} entries</span></div><div className="eventList">{memories.length===0?<p className="emptyState">No memories stored yet.</p>:memories.map(m=><article className="eventItem" key={m.id}><div><strong>{m.title}</strong><p>{m.scope} · {m.memoryType}</p><span>{m.content}</span></div><div className="auditMeta"><span>{m.source??"system"}</span><time>{new Date(m.createdAtUtc).toLocaleString("en-GB")}</time></div></article>)}</div></section></main>}
